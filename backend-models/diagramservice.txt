diagramservice.php, reads github activity data for a user (Including number of issues created, comments posted, lines of code modified) for every week and returns it in a JSON object\

Modelled code blocks as methods

Input:

userid
loginname
lastname
firstname
log_uuid

opt
courseid
coursename

Output:
JSON Array
